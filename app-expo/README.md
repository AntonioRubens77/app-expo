# App Expo

Aplicativo de estudos em React Native criado com Expo.

## Executar o projeto

```bash
npm install
npm start
```

Também estão disponíveis os comandos:

- `npm run android`
- `npm run ios`
- `npm run web`

O componente principal está em `App.js`, e as telas ficam em `src/pages/`.
